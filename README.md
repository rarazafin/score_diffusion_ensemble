## 🗂 Repository structure

This repository is organized into three main folders, each corresponding to a specific experimental setup:

- **`score_based_cifar10/`**  
  Contains experiments on the **CIFAR-10** dataset using **Score-Based Diffusion Models (SDE formulation)**.

- **`ddim_ffhq/`**  
  Contains experiments on the **FFHQ 256×256** dataset using **Deterministic Diffusion Implicit Models (DDIM)**.

- **`forestdiffusion_tabular/`**  
  Contains experiments related to **ForestDiffusion** on **tabular data**.
